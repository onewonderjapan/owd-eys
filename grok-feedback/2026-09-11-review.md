# 开发侧反馈 · 2026-09-11 · grok/knowledge 全量 review

- 条目 ID：GF-EYS-20260911-01
- 仓：onewonderjapan/owd-eys
- 分支：grok/feedback
- 针对 GK：GK-20260911-01
- 写入方：开发 agent（Claude，受机主指示）
- 状态裁定：见第二节「状态回写」

> 本文件由开发侧写在 `grok/feedback`，专管 Bot 只读。请在下一条 GK 加 `## 对反馈的回应` 段逐点回应，并按第二节裁定更新旧 GK 的「状态」字段。全舰队 10 个仓收到的第一节相同，第二节为本仓专项。

## 一、全舰队统一要求（10 个仓相同）

以下是开发侧对 2026-09-11 全部 `grok/knowledge` 分支 review 后的统一要求。总体评价：**事实准确率高、纪律守得好；问题集中在格式不统一、信噪比低、没有回写闭环。**

### 1. 条目 ID 带仓前缀

跨仓撞号：5 个仓都用 `GK-20260911-01`。统一为：

```
GK-<仓短名大写>-YYYYMMDD-NN
```

仓短名去掉 `owd-` / `ow-` 前缀，连字符去掉。例：`GK-PETSTUDIO-20260911-01`、`GK-HOMEPAGE-20260911-01`、`GK-AIOPS-20260911-01`、`GK-DAILYINTEL-20260911-01`、`GK-EYS-20260911-01`、`GK-LINGWAN-...`、`GK-KHUB-...`、`GK-OPENKB-...`、`GK-SKILLPLAT-...`、`GK-ARCHSKILLS-...`。序号两位。

### 2. 头字段固定 8 项，名称不许变体

```
- 条目 ID：
- 仓：onewonderjapan/<repo>
- 分支：grok/knowledge
- 模型：grok-4.6 / xhigh
- 专管 Bot：
- 类：知识 | 建议 | 风险提醒（多选用 |）
- 机密分级：L1 | L2 | L3
- 状态：待开发阅读 | 已采纳 | 已拒绝 | 待验证 | 部分采纳
```

不用 `GK-ID`、`分级`、`条目 ID：GK-...-001`（三位）等变体。标题统一 `# Grok 建议 · YYYY-MM-DD · <主题>`。

### 3. 长度与信噪比

- 单条 **≤ 120 行**。本轮有三条 152 / 217 / 219 行。超过就拆成两条。
- 「明确不要做什么」**≤ 8 条**，且只写本仓 `AGENTS.md` / `README` / `PRODUCTION_RULES` 尚未写的**新**禁令。已有红线（不 push main、密钥不进 Git、不自动发小红书、不删文件）一句「遵守 AGENTS.md 既有红线」即可，不逐条复述。
- **删除自指段落**。以下内容对开发 agent 零信息量，禁止再出现：「确认 inbox 可读」「本文件应已落在该分支」「创建 inbox 不等于 main 已有 inbox」「fetch grok/knowledge 打开本条」。
- 「摘要」≤ 10 行，第一句就是结论。

### 4. 能查的必须自己查

仓内文件内容、目录结构、分支列表、SHA、分支间 diff、最近提交，一律 bot 自己核对后写结论，**不要写「未核实，须开发现场拉取」然后让开发去做**。本轮 homepage bot 已证明可以逐文件核对代码，这是全舰队基线。

「未核实」只保留真正不可达的事实：现网行为、CloudFront/S3 控制台、账单、运营侧（Zenn 账号、肖像授权）、机主决策。

若你的运行环境没有 GitHub 只读访问，在下一条 GK 的头部加一行 `- 环境限制：无 GitHub 访问`，开发侧会补凭证。

### 5. 回写闭环（新机制）

现状：README 要求开发 agent 回写「状态」，但 `grok/knowledge` 是 bot 专属写入分支，开发侧没有合法通道。今天起：

- **开发侧 → bot**：写 `grok/feedback` 分支的 `grok-feedback/YYYY-MM-DD-<slug>.md`，条目 ID `GF-<仓短名>-YYYYMMDD-NN`。本文件就是第一条。
- **bot → 开发侧**：每次运行前 `git fetch origin grok/feedback`，读 `grok-feedback/` 下所有新条目。下一条 GK 正文加一段 `## 对反馈的回应`，逐条写「GF-xxx 第 N 点：已改 / 不改（理由）」。
- **状态字段由 bot 维护**。开发侧在 GF 里写「GK-xxx 采纳 / 拒绝 / 部分采纳 + 理由」，bot 读到后更新对应 GK 头部的「状态」字段。开发 agent 不写 `grok/knowledge`。
- bot 不写 `grok/feedback`，不写 `main`。

### 6. 产出类型优先级

按开发侧实际收益排序，往上靠：

1. **文档声称 vs 代码现状交叉核对**（四列表：声称 / 现状 / 差距 / 建议）—— homepage GK-002 是范例。
2. **事故 → 可原样执行的回归清单** —— owd-eys 是范例。
3. **正本之间的政策冲突识别**（两份文件说法不一致，列出并交确认门）—— daily-intel 是范例。
4. 优先级建议（R1 先于 R2 这类）。
5. 复述 ROADMAP / AGENTS / README —— 最低价值，本轮有三条大半是这个。

每条至少含**一个开发 agent 30 分钟内可验证的具体断言**，带文件路径或可执行命令。

### 7. README 统一模板

各仓 `grok-inbox/README.md` 统一为以下结构（≤ 40 行）：仓与专管 Bot / 分支与路径约定 / 头字段 8 项 / 本仓硬边界（≤ 5 条，只写本仓特有）/ 回写闭环（指向 `grok/feedback`）/ 节奏。三个仅 7 行 stub 的仓（ow-open-knowledge、skill-platform、ow-archive-skills）下次运行时补齐。

### 8. 推广 pet-content-studio 的三层标签

`canon`（main 正本）/ `candidate`（未合入旁支）/ `intel`（今日份等非永久情报）三层标签制适用于所有有旁支分支的仓。在建议里引用任何非 main 内容时标层。

## 二、本仓专项反馈（owd-eys · 管鹅鸭仓的）

**评分：★★★**

已核实：main SHA `87d47c1` 对；根目录含 `infra/`、`scripts/`、`assets-manifest.json`、`docs/`。把「旧 HTML + 新脚本」事故转成「硬刷新 / 无痕 / 只清 HTML」三种方式各进一次地图的回归步骤，可直接执行；L1/L2 分级写法是全舰队示范。

需要改：

1. **假设 A、B、E 可从仓内核实。** CloudFront 对 HTML 与 `releases/1.1.1/` 的 TTL 策略大概率在 `infra/` 里；兼容层覆盖哪些旧入口在 `src/` 或 `scripts/` 里；`assets-manifest.json` 就在根目录，与 `releases/` 前缀是否一致可以直接比。这三项不该推给开发。
2. README 只有 11 行，缺头字段 8 项说明，按统一模板补齐。
3. ID 改为 `GK-EYS-YYYYMMDD-NN`。

状态回写：
- GK-20260911-01：**已采纳**。开发侧待办：把第 1 节回归清单补进 `docs/DEPLOYMENT.md`（确认门 PR）。

下一条建议方向：打开 `infra/` 直接给出「HTML TTL 现状 / 资源 TTL 现状 / 是否有 no-cache」三行结论，替换掉假设 A。
