# Grok 建议 · 2026-09-11 · 1.1.1 上线一周：缓存钉扎回归清单与实验轨卫生闸门

- 条目 ID：GK-20260911-01
- 仓：onewonderjapan/owd-eys
- 分支：grok/knowledge
- 模型：grok-4.6 / xhigh
- 专管 Bot：管鹅鸭仓的
- 类：知识 | 建议 | 风险提醒
- 机密分级：L1（公开站点可见事实：入口、版本、玩法边界、非官方声明）；L2（私有 GitHub 组织、S3/CloudFront OAC、哈希清单、访问保护与成本口径，勿写入公开页）
- 状态：待开发阅读

## 摘要

公开入口 `https://eys.onewonder.co.jp/` 当前为 **1.1.1**（Three.js 0.180.0）：选角 → 小镇漫游（俯视 / 第一人称），27 角色，单人探索，无联机、无完整规则。`main` 与 `grok/knowledge` 同 SHA `87d47c1`，尚无 `grok-inbox/`。已知事故是**旧 HTML 缓存与新脚本混用导致进地图失败**，已用 `releases/1.1.1/` 版本钉扎 + 兼容层修复。上线后第一周最高杠杆不是加玩法，而是把这次事故固化成**可重复执行的发布卫生清单**，并锁死实验轨边界（勿与正式产品仓混写、勿共享发布管线）。优先低风险：文档化回归步骤、核对钉扎是否仍生效、核对声明与访问保护仍在、记录成本与缓存命中，而不是改资产管线或清历史对象。

## 依据（可核对引用 / 未核实假设须标明）

**已核实（2026-09-11）**

- 公开入口与产品形态：`https://eys.onewonder.co.jp/`；版本 1.1.1；Three.js 0.180.0；模块为选角 → 小镇漫游（俯视/第一人称）；27 角色；单人探索；无联机/完整规则。
- 代码状态：`main` 与 `grok/knowledge` 同 SHA `87d47c1`；仓库内尚无 `grok-inbox/`。
- 最近提交主题：Publish noncommercial EYS playground；Thicken flower petals + first-person；Fix cached-page map entry and pin release assets。
- 资产与基础设施：GLB / 头像在私有 S3，哈希清单 `assets-manifest.json`；代码在私有 GitHub；独立 S3 + CloudFront OAC + HTTPS + Route53；复用 pet 站点证书 / 缓存 / 安全头**模式**，且明确不改 pet。
- 已知事故与修复：旧 HTML 缓存与新脚本混用 → 进入地图失败；已用 `releases/1.1.1/` 版本钉扎 + 兼容层修复。
- 文档：`ACCESS_PROTECTION.md`、`DEPLOYMENT.md`、`IMPLEMENTATION.md`。
- 声明必须保留：非官方二创、技术验证、娱乐、非商用。
- 本轨定位：鹅鸭杀非官方二创「鹅教堂漫游」实验/娱乐验证轨。

**未核实假设（开发 agent 执行前须对照文档与控制台，不得当事实写进公开页）**

- 假设 A：CloudFront 对 HTML / `index` 与对 `releases/1.1.1/` 静态资源的 TTL / 缓存键策略可能不同；若 HTML 仍被长缓存，同类混用事故可复发。
- 假设 B：兼容层只覆盖「旧 HTML + 新脚本」这一条路径，未必覆盖「新 HTML + 旧 chunk」或 Service Worker / bfcache。
- 假设 C：第一人称与移动端（触控、陀螺、低端 GPU、竖屏）在 1.1.1 发布后尚未做系统回归；花瓣加厚可能影响 fill rate（未测）。
- 假设 D：访问保护（见 `ACCESS_PROTECTION.md`）与 OAC 当前有效，但上线一周后可能出现热链、扫描或费用爬升，需看账单与 4xx/5xx，不能从公开页反推。
- 假设 E：`assets-manifest.json` 与 S3 对象哈希一致；公开页未暴露清单内容，一致性只能在私有侧核对。

## 建议开发 agent 采取的下一步

在 **`grok/knowledge` 工作树**做只读核对 + 文档补丁（不要宣称已进 `main`）。优先顺序如下，全部可在一天内完成、不碰历史对象、不把大资产进 Git。

1. **写一份「1.1.1 缓存 / 钉扎回归清单」进现有部署文档（补 `DEPLOYMENT.md` 一节，或同目录短文，勿新开产品仓）**  
   清单必须能被下次发版原样执行，至少包含：
   - 打开公开入口，看页面实际加载的脚本 / 资源路径是否全部落在 `releases/1.1.1/`（或文档写明的钉扎前缀），而不是无版本根路径。
   - 硬刷新、无痕、以及「只清 HTML 不清资源」三种方式各进一次地图（选角 → 漫游），确认不再出现旧 HTML + 新脚本失败。
   - 核对 `assets-manifest.json` 中的哈希与当前钉扎目录对象一致（私有侧，L2）。
   - 记录 CloudFront 行为：HTML 是否 `no-cache` / 短 TTL；带哈希或 `releases/1.1.1/` 的资源是否可长缓存。若文档与控制台不一致，只在 L2 笔记里标差异，不改 pet、不擅自改生产 TTL。
   - 明确「兼容层」覆盖哪些旧入口 URL；列出**未覆盖**路径（假设 B）为后续工单，本周不实现。

2. **上线一周卫生快照（只读，写入 `grok/knowledge` 笔记，不上公开站）**  
   - 访问保护：对照 `ACCESS_PROTECTION.md`，确认公开页仍只暴露 L1；OAC / 私有桶未误开公开 list。
   - 成本：导出本周 CloudFront + S3 量级（流量、请求数、4xx）；若出现异常热链，先加监控/告警描述，**不要**删历史对象。
   - 声明：公开页仍有非官方 / 非商用 / 技术验证 / 娱乐声明。
   - 边界：确认本仓发布管线未与正式产品仓共用；证书/缓存模式是「复用模式」而非改 pet 配置。

3. **体验验证只排优先级，本周不扩玩法**  
   - P0：桌面俯视进图、桌面第一人称进图、一次完整选角（抽 2–3 个角色即可，27 全量可后置）。
   - P1：移动端竖屏选角 + 漫游（触控相机、点击进图）；记录卡死/黑屏/无法退出第一人称。
   - P2：花瓣加厚后的低端机帧时间（假设 C，未测则保持未测标签）。  
   产出是「通过 / 失败 / 未测」表，不是新功能。

4. **知识轨落点**  
   因尚无 `grok-inbox/`，本条保持为 `grok/knowledge` 上的建议文件，供开发 agent 阅读。不要把 L2（桶名、OAC、账单、清单哈希）复制到公开 HTML。

## 明确不要做什么

- 不要建议或执行与正式产品仓混写、共用 CI/CD、共用 CloudFront distribution、或改 pet 站点配置。
- 不要假装本建议或任何修复已 merge 进 `main`；当前仅知 `main` 与 `grok/knowledge` 同为 `87d47c1`。
- 不要删除历史 S3 / CloudFront 对象「以清理缓存」；钉扎目录与旧对象保留，缓存问题用缓存策略与版本前缀解决。
- 不要把 GLB、头像、Blender 源文件提交进 Git；继续走私有 S3 + `assets-manifest.json`。
- 不要本周做联机、完整鹅鸭规则、或把 27 角色做成可发布的「正式玩法」；本轨仍是单人探索验证。
- 不要在公开站点暴露 L2：私有 GitHub 路径细节、桶策略、OAC、清单哈希、访问密钥、成本数字。
- 不要去掉非官方 / 非商用 / 技术验证 / 娱乐声明。
- 不要为「验证第一人称」去重做资产管线或加厚更多网格；先测再改。
- 未核实的缓存 TTL、兼容层覆盖范围、移动端表现、费用异常，必须保持「假设 / 未测」，禁止写成已证实结论。
